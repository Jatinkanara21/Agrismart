import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'package:image_picker/image_picker.dart';
import 'package:fl_chart/fl_chart.dart';
import '../../providers/agri_provider.dart';
import '../../widgets/agri_widgets.dart';
import '../../core/theme/app_theme.dart';

class DashboardShell extends StatefulWidget { const DashboardShell({super.key}); @override State<DashboardShell> createState()=>_DashboardShellState(); }
class _DashboardShellState extends State<DashboardShell> {
  int index=0;
  final pages=<String>['Home','My Farm','AI','Market','Profile'];
  @override void initState(){super.initState(); WidgetsBinding.instance.addPostFrameCallback((_)=>context.read<AgriProvider>().refresh());}
  @override Widget build(BuildContext context){ final p=context.watch<AgriProvider>(); return Scaffold(body: IndexedStack(index:index,children:[HomePage(onFeature:_open), FarmPage(onOpen:_open), AiHub(onOpen:_open), MarketPage(), ProfilePage()]), bottomNavigationBar:NavigationBar(selectedIndex:index,onDestinationSelected:(v)=>setState(()=>index=v),destinations:const [NavigationDestination(icon:Icon(Icons.home_outlined),selectedIcon:Icon(Icons.home),label:'Home'),NavigationDestination(icon:Icon(Icons.agriculture_outlined),selectedIcon:Icon(Icons.agriculture),label:'My Farm'),NavigationDestination(icon:Icon(Icons.auto_awesome_outlined),selectedIcon:Icon(Icons.auto_awesome),label:'AI'),NavigationDestination(icon:Icon(Icons.show_chart_outlined),selectedIcon:Icon(Icons.show_chart),label:'Market'),NavigationDestination(icon:Icon(Icons.person_outline),selectedIcon:Icon(Icons.person),label:'Profile')]),floatingActionButton:index==0?FloatingActionButton.extended(backgroundColor:AppTheme.forest,onPressed:()=>_open('AgriBot'),icon:const Icon(Icons.mic,color:Colors.white),label:const Text('AgriBot',style:TextStyle(color:Colors.white))):null); }
  void _open(String name){ Navigator.of(context).push(MaterialPageRoute(builder:(_)=>FeaturePage(title:name))); }
}

class TopBar extends StatelessWidget { const TopBar({super.key,required this.title,required this.subtitle}); final String title,subtitle; @override Widget build(BuildContext context)=>Row(children:[CircleAvatar(radius:24,backgroundColor:AppTheme.green.withOpacity(.12),child:const Icon(Icons.eco,color:AppTheme.green)),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(title,style:const TextStyle(fontSize:20,fontWeight:FontWeight.w900)),Text(subtitle,style:const TextStyle(fontSize:12,color:Colors.black54))])),IconButton(onPressed:(){},icon:const Icon(Icons.notifications_none)),IconButton(onPressed:(){},icon:const Icon(Icons.location_on_outlined))]); }

class HomePage extends StatelessWidget { const HomePage({super.key,required this.onFeature}); final void Function(String) onFeature; @override Widget build(BuildContext context){final p=context.watch<AgriProvider>(); return SafeArea(child:RefreshIndicator(onRefresh:p.refresh,child:ListView(padding:const EdgeInsets.fromLTRB(18,18,18,100),children:[const TopBar(title:'Good morning, Jatin',subtitle:'Green Valley Farm • Ahmedabad'),const SizedBox(height:18),AgriCard(child:Container(decoration:BoxDecoration(gradient:LinearGradient(colors:[AppTheme.forest,AppTheme.green]),borderRadius:BorderRadius.circular(16)),padding:const EdgeInsets.all(20),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text("TODAY'S FARMING INTELLIGENCE",style:TextStyle(color:Colors.white70,fontSize:11,fontWeight:FontWeight.w800,letterSpacing:1.1)),const SizedBox(height:8),const Text('Know what to do next.',style:TextStyle(color:Colors.white,fontWeight:FontWeight.w900,fontSize:24)),const SizedBox(height:16),Row(children:[const Icon(Icons.thermostat,color:Colors.white70),const SizedBox(width:6),Text('${p.weather['temperature']}°C • ${p.weather['condition']}',style:const TextStyle(color:Colors.white)),const Spacer(),Text('${p.weather['rainProbability']}% rain',style:const TextStyle(color:Colors.white,fontWeight:FontWeight.w800))])])),const SizedBox(height:16),Row(children:[Expanded(child:MetricTile(icon:Icons.water_drop_outlined,title:'Soil Moisture',value:'${p.farm.moisture.round()}%',subtitle:'Target 40–60%')),const SizedBox(width:12),Expanded(child:MetricTile(icon:Icons.eco_outlined,title:'Soil Health',value:'87/100',subtitle:'Good condition'))]),const SizedBox(height:18),const Text('Recommended actions',style:TextStyle(fontSize:18,fontWeight:FontWeight.w900)),const SizedBox(height:10),if(p.insights.isEmpty) const LinearProgressIndicator() else ...p.insights.take(4).map((i)=>Padding(padding:const EdgeInsets.only(bottom:10),child:AgriCard(child:Row(children:[CircleAvatar(backgroundColor:AppTheme.green.withOpacity(.1),child:Icon(IconData(i.icon,fontFamily:'MaterialIcons'),color:AppTheme.green)),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[Expanded(child:Text(i.title,style:const TextStyle(fontWeight:FontWeight.w800))),Text(i.level,style:TextStyle(fontSize:11,color:i.level=='High'?Colors.red:AppTheme.green,fontWeight:FontWeight.w700))]),const SizedBox(height:4),Text(i.message,style:const TextStyle(fontSize:12,color:Colors.black54))]))]))),const SizedBox(height:8),const Text('AI tools',style:TextStyle(fontSize:18,fontWeight:FontWeight.w900)),const SizedBox(height:10),GridView.count(shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),crossAxisCount:2,mainAxisSpacing:10,crossAxisSpacing:10,childAspectRatio:1.25,children:[FeatureTile(icon:Icons.spa,title:'Recommend Crop',subtitle:'Find your best crop',onTap:()=>onFeature('Crop Recommendation')),FeatureTile(icon:Icons.camera_alt_outlined,title:'Detect Disease',subtitle:'Scan plant health',onTap:()=>onFeature('Disease Detection')),FeatureTile(icon:Icons.analytics_outlined,title:'Predict Yield',subtitle:'Estimate harvest',onTap:()=>onFeature('Yield Prediction')),FeatureTile(icon:Icons.smart_toy_outlined,title:'Ask AgriBot',subtitle:'Get farming advice',onTap:()=>onFeature('AgriBot'))]),const SizedBox(height:18),FeatureTile(icon:Icons.water_drop_outlined,title:'Smart Irrigation',subtitle:'Moisture + rain aware',onTap:()=>onFeature('Smart Irrigation')),const SizedBox(height:10),FeatureTile(icon:Icons.science_outlined,title:'Fertilizer Plan',subtitle:'NPK-aware guidance',onTap:()=>onFeature('Fertilizer Recommendation'))])); }
}

class FarmPage extends StatelessWidget { const FarmPage({super.key,required this.onOpen}); final void Function(String) onOpen; @override Widget build(BuildContext context){final f=context.watch<AgriProvider>().farm; return SafeArea(child:ListView(padding:const EdgeInsets.all(18),children:[const TopBar(title:'My Farm',subtitle:'Manage fields, crops and history'),const SizedBox(height:16),AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[const Icon(Icons.agriculture,color:AppTheme.green),const SizedBox(width:10),Expanded(child:Text(f.name,style:const TextStyle(fontSize:20,fontWeight:FontWeight.w900))),Chip(label:Text('${f.area} ha'))]),const SizedBox(height:14),Wrap(spacing:8,runSpacing:8,children:[Chip(label:Text(f.location)),Chip(label:Text(f.soil)),Chip(label:Text(f.crop)),Chip(label:Text('Moisture ${f.moisture.round()}%'))]),const SizedBox(height:14),LinearProgressIndicator(value:.68,minHeight:8,borderRadius:BorderRadius.circular(20)),const SizedBox(height:8),const Text('Crop timeline • Growth stage: Flowering',style:TextStyle(fontSize:12,color:Colors.black54))])),const SizedBox(height:16),FeatureTile(icon:Icons.add_circle_outline,title:'Add another farm',subtitle:'Track multiple fields independently',onTap:()=>onOpen('Add Farm')),const SizedBox(height:10),FeatureTile(icon:Icons.timeline,title:'Crop management',subtitle:'Schedule, growth stages and harvest',onTap:()=>onOpen('Crop Management')),const SizedBox(height:10),FeatureTile(icon:Icons.science,title:'Soil health',subtitle:'NPK, pH, moisture and organic matter',onTap:()=>onOpen('Soil Health'))]));}}

class AiHub extends StatelessWidget { const AiHub({super.key,required this.onOpen}); final void Function(String) onOpen; @override Widget build(BuildContext context)=>SafeArea(child:ListView(padding:const EdgeInsets.all(18),children:[const TopBar(title:'AI Farming',subtitle:'Five intelligence systems in one place'),const SizedBox(height:16),const Text('Pick an AI tool',style:TextStyle(fontSize:18,fontWeight:FontWeight.w900)),const SizedBox(height:10),...['Crop Recommendation','Disease Detection','Yield Prediction','AgriBot','Decision Engine'].map((x)=>Padding(padding:const EdgeInsets.only(bottom:10),child:FeatureTile(icon:Icons.auto_awesome,title:x,subtitle:'AI-powered analysis with explanations and actions',onTap:()=>onOpen(x))))])); }

class MarketPage extends StatelessWidget { const MarketPage({super.key}); @override Widget build(BuildContext context){final prices=context.watch<AgriProvider>().marketPrices; return SafeArea(child:ListView(padding:const EdgeInsets.all(18),children:[const TopBar(title:'Market Intelligence',subtitle:'Nearby prices and trends'),const SizedBox(height:16),if(prices.isEmpty)const LinearProgressIndicator() else ...prices.map((x)=>Padding(padding:const EdgeInsets.only(bottom:10),child:AgriCard(child:Row(children:[Container(width:46,height:46,decoration:BoxDecoration(color:AppTheme.green.withOpacity(.1),borderRadius:BorderRadius.circular(14)),child:const Icon(Icons.grass,color:AppTheme.green)),const SizedBox(width:12),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(x['crop'],style:const TextStyle(fontWeight:FontWeight.w800)),Text('${x['market']} • ₹${x['price']}/q',style:const TextStyle(fontSize:12,color:Colors.black54))])),Text(x['trend'],style:TextStyle(fontWeight:FontWeight.w800,color:x['trend'].toString().startsWith('+')?AppTheme.green:Colors.red))]))),const SizedBox(height:8),AgriCard(child:SizedBox(height:220,child:LineChart(LineChartData(gridData:const FlGridData(show:false),titlesData:const FlTitlesData(show:false),borderData:FlBorderData(show:false),lineBarsData:[LineChartBarData(isCurved:true,barWidth:4,dotData:const FlDotData(show:false),spots:const [FlSpot(0,2.4),FlSpot(1,2.7),FlSpot(2,2.6),FlSpot(3,3.2),FlSpot(4,3.1),FlSpot(5,3.5)])]))),const SizedBox(height:10),const Text('AI note: Prices move with supply, quality, arrivals and local demand. Treat trends as guidance, not a guaranteed selling price.',style:TextStyle(fontSize:12,color:Colors.black54))]));}}

class ProfilePage extends StatelessWidget { const ProfilePage({super.key}); @override Widget build(BuildContext context)=>SafeArea(child:ListView(padding:const EdgeInsets.all(18),children:[const TopBar(title:'Profile & Settings',subtitle:'Your farm preferences'),const SizedBox(height:16),AgriCard(child:Row(children:[const CircleAvatar(radius:30,backgroundColor:AppTheme.green,child:Icon(Icons.person,color:Colors.white,size:32)),const SizedBox(width:14),const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Jatin',style:TextStyle(fontWeight:FontWeight.w900,fontSize:19)),Text('Farmer • Ahmedabad, Gujarat',style:TextStyle(color:Colors.black54))])])),const SizedBox(height:12),FeatureTile(icon:Icons.language,title:'Language',subtitle:'English • Hindi • Gujarati • Marathi +6',onTap:(){_showLanguage(context);}),const SizedBox(height:10),FeatureTile(icon:Icons.notifications_outlined,title:'Notifications',subtitle:'Weather, disease, irrigation and market alerts',onTap:(){}),const SizedBox(height:10),FeatureTile(icon:Icons.verified_user_outlined,title:'Privacy & security',subtitle:'Permissions, data controls and account security',onTap:(){}),const SizedBox(height:10),FeatureTile(icon:Icons.menu_book_outlined,title:'Government schemes',subtitle:'Official sources, eligibility and application steps',onTap:()=>Navigator.push(context,MaterialPageRoute(builder:(_)=>const FeaturePage(title:'Government Schemes'))))]));}
void _showLanguage(BuildContext c){showModalBottomSheet(context:c,builder:(_)=>Padding(padding:const EdgeInsets.all(20),child:Wrap(children:['English','Hindi','Gujarati','Marathi','Punjabi','Bengali','Tamil','Telugu','Kannada','Malayalam'].map((l)=>ListTile(title:Text(l),onTap:()=>Navigator.pop(c))).toList())));}
}

class FeaturePage extends StatelessWidget { const FeaturePage({super.key,required this.title}); final String title; @override Widget build(BuildContext context){final p=context.watch<AgriProvider>(); Widget body; switch(title){case 'Crop Recommendation':body=const CropRecView();break;case 'Disease Detection':body=const DiseaseView();break;case 'Yield Prediction':body=const YieldView();break;case 'AgriBot':body=const BotView();break;case 'Decision Engine':body=const DecisionView();break;case 'Weather':body=const WeatherView();break;case 'Soil Health':body=const SoilView();break;case 'Smart Irrigation':body=const IrrigationView();break;case 'Fertilizer Recommendation':body=const FertilizerView();break;case 'Government Schemes':body=const SchemesView();break;default:body=GenericView(title:title);}
return Scaffold(appBar:AppBar(title:Text(title)),body:body);}}

class CropRecView extends StatelessWidget{const CropRecView({super.key});@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();return ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Field profile',style:TextStyle(fontWeight:FontWeight.w900,fontSize:18)),SizedBox(height:6),Text('N 82 • P 48 • K 42 • pH 6.7 • 29°C • 78% humidity • 185 mm rainfall',style:TextStyle(color:Colors.black54))])),const SizedBox(height:12),FilledButton.icon(onPressed:p.recommendCrop,icon:const Icon(Icons.auto_awesome),label:const Text('Run crop recommendation')),const SizedBox(height:12),if(p.busy)const LinearProgressIndicator() else ...p.cropRecommendations.map((r)=>Padding(padding:const EdgeInsets.only(bottom:10),child:AgriCard(child:Row(children:[ScoreRing(value:r.score,label:'fit'),const SizedBox(width:14),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(r.crop,style:const TextStyle(fontSize:18,fontWeight:FontWeight.w900)),Text(r.reason,style:const TextStyle(fontSize:12,color:Colors.black54)),const SizedBox(height:6),Text('${r.duration} • Water ${r.water}',style:const TextStyle(fontWeight:FontWeight.w700,fontSize:12))]))]))))]));}}

class DiseaseView extends StatelessWidget{const DiseaseView({super.key});@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();return ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:Column(children:[const Icon(Icons.local_florist,color:AppTheme.green,size:60),const SizedBox(height:8),const Text('Scan a clear leaf or crop image',style:TextStyle(fontWeight:FontWeight.w900,fontSize:18)),const SizedBox(height:6),const Text('The model is API-ready for MobileNet/EfficientNet/CNN inference.',textAlign:TextAlign.center,style:TextStyle(color:Colors.black54)),const SizedBox(height:16),Row(children:[Expanded(child:OutlinedButton.icon(onPressed:()=>p.scanDisease(source:ImageSource.camera),icon:const Icon(Icons.camera_alt),label:const Text('Camera'))),const SizedBox(width:10),Expanded(child:OutlinedButton.icon(onPressed:()=>p.scanDisease(source:ImageSource.gallery),icon:const Icon(Icons.photo_library),label:const Text('Gallery')))])])),const SizedBox(height:12),if(p.busy)const LinearProgressIndicator() else if(p.disease!=null)AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[ScoreRing(value:p.disease!.confidence,label:'conf.'),const SizedBox(width:14),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(p.disease!.name,style:const TextStyle(fontSize:20,fontWeight:FontWeight.w900)),Text('Severity: ${p.disease!.severity}',style:const TextStyle(fontWeight:FontWeight.w800,color:Colors.orange))]))]),const Divider(height:28),const Text('Likely cause',style:TextStyle(fontWeight:FontWeight.w900)),Text(p.disease!.cause),const SizedBox(height:12),const Text('Recommended action',style:TextStyle(fontWeight:FontWeight.w900)),Text(p.disease!.action),const SizedBox(height:12),const Text('Use this as decision support; confirm disease and chemical treatment locally before application.',style:TextStyle(fontSize:12,color:Colors.black54))]))]);}}

class YieldView extends StatelessWidget{const YieldView({super.key});@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();return ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:const Text('Wheat • 4.8 ha • Moisture 31% • Fertilizer index 70 • Historical yield 4.35 t/ha')),const SizedBox(height:12),FilledButton.icon(onPressed:p.predictYield,icon:const Icon(Icons.analytics_outlined),label:const Text('Predict yield')),const SizedBox(height:12),if(p.yield!=null)AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[ScoreRing(value:p.yield!.confidence,label:'conf.'),const SizedBox(width:14),Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('${p.yield!.estimated.toStringAsFixed(1)} t/ha',style:const TextStyle(fontSize:27,fontWeight:FontWeight.w900)),const Text('Estimated yield',style:TextStyle(color:Colors.black54))])]),const SizedBox(height:20),SizedBox(height:220,child:BarChart(BarChartData(barGroups:[BarChartGroupData(x:0,barRods:[BarChartRodData(toY:4.35,width:26)]),BarChartGroupData(x:1,barRods:[BarChartRodData(toY:p.yield!.estimated,width:26)]),BarChartGroupData(x:2,barRods:[BarChartRodData(toY:p.yield!.max,width:26)])],titlesData:const FlTitlesData(show:false),borderData:FlBorderData(show:false),gridData:const FlGridData(show:false)))),Text('Expected range: ${p.yield!.min.toStringAsFixed(1)}–${p.yield!.max.toStringAsFixed(1)} t/ha',style:const TextStyle(fontWeight:FontWeight.w800))]))]);}}

class BotView extends StatefulWidget{const BotView({super.key});@override State<BotView>createState()=>_BotViewState();}class _BotViewState extends State<BotView>{final ctl=TextEditingController();@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();return Column(children:[Expanded(child:ListView(padding:const EdgeInsets.all(18),children:[if(p.messages.isEmpty)const AgriCard(child:Column(children:[Icon(Icons.smart_toy,color:AppTheme.green,size:54),SizedBox(height:8),Text('Ask AgriBot anything',style:TextStyle(fontSize:20,fontWeight:FontWeight.w900)),SizedBox(height:6),Text('Try: “My wheat leaves are turning yellow. What should I check?”',textAlign:TextAlign.center,style:TextStyle(color:Colors.black54))])) else ...p.messages.map((m)=>Align(alignment:m.isUser?Alignment.centerRight:Alignment.centerLeft,child:Container(margin:const EdgeInsets.only(bottom:10),padding:const EdgeInsets.all(14),constraints:const BoxConstraints(maxWidth:340),decoration:BoxDecoration(color:m.isUser?AppTheme.green:Colors.white,borderRadius:BorderRadius.circular(18)),child:Text(m.text,style:TextStyle(color:m.isUser?Colors.white:Colors.black87)))))])),SafeArea(child:Padding(padding:const EdgeInsets.all(12),child:Row(children:[Expanded(child:TextField(controller:ctl,decoration:const InputDecoration(hintText:'Ask about your farm…'))),const SizedBox(width:8),IconButton.filled(onPressed:(){final t=ctl.text.trim();if(t.isNotEmpty){p.askBot(t);ctl.clear();}},icon:const Icon(Icons.send))])))]);}}

class DecisionView extends StatelessWidget{const DecisionView({super.key});@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();return ListView(padding:const EdgeInsets.all(18),children:[const Text('Smart Decision Engine',style:TextStyle(fontSize:22,fontWeight:FontWeight.w900)),const SizedBox(height:6),const Text('Combines weather, soil, crop, AI outputs and your farm history into a few actions.',style:TextStyle(color:Colors.black54)),const SizedBox(height:16),...p.insights.map((i)=>Padding(padding:const EdgeInsets.only(bottom:10),child:AgriCard(child:ListTile(contentPadding:EdgeInsets.zero,leading:CircleAvatar(backgroundColor:AppTheme.green.withOpacity(.1),child:const Icon(Icons.auto_awesome,color:AppTheme.green)),title:Text(i.title,style:const TextStyle(fontWeight:FontWeight.w900)),subtitle:Padding(padding:const EdgeInsets.only(top:4),child:Text(i.message))))))]);}}
class WeatherView extends StatelessWidget{const WeatherView({super.key});@override Widget build(BuildContext c){final w=c.watch<AgriProvider>().weather;return ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('${w['temperature']}°C',style:const TextStyle(fontSize:46,fontWeight:FontWeight.w900)),Text(w['condition']),const SizedBox(height:16),Wrap(spacing:18,runSpacing:12,children:[Text('Humidity ${w['humidity']}%'),Text('Rain ${w['rainProbability']}%'),Text('Wind ${w['wind']} km/h'),Text('UV ${w['uv']}')])])),const SizedBox(height:12),const AgriCard(child:Text('AI farming advice: rain probability above 70% means irrigation may be unnecessary today; postpone fertilizer when heavy rain could wash nutrients away.'))]);}}
class SoilView extends StatelessWidget{const SoilView({super.key});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(18),children:[const ScoreRing(value:.87,label:'health'),const SizedBox(height:18),Row(children:[const Expanded(child:MetricTile(icon:Icons.science_outlined,title:'pH',value:'6.7',subtitle:'Good for wheat')),const SizedBox(width:10),const Expanded(child:MetricTile(icon:Icons.nature_outlined,title:'N',value:'82',subtitle:'kg/ha index'))]),const SizedBox(height:10),Row(children:[const Expanded(child:MetricTile(icon:Icons.bolt,title:'P',value:'48',subtitle:'kg/ha index')),const SizedBox(width:10),const Expanded(child:MetricTile(icon:Icons.local_fire_department_outlined,title:'K',value:'42',subtitle:'kg/ha index'))])]);}
class IrrigationView extends StatelessWidget{const IrrigationView({super.key});@override Widget build(BuildContext c){final p=c.watch<AgriProvider>();final recommended=p.farm.moisture<35&&p.weather['rainProbability']<60;return ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(recommended?'Irrigation recommended':'Hold irrigation today',style:const TextStyle(fontSize:24,fontWeight:FontWeight.w900)),const SizedBox(height:10),Text('Current moisture ${p.farm.moisture.round()}% • rain probability ${p.weather['rainProbability']}%'),const SizedBox(height:14),LinearProgressIndicator(value:p.farm.moisture/100,minHeight:10),const SizedBox(height:10),const Text('Target moisture: 40–60%',style:TextStyle(color:Colors.black54)),const SizedBox(height:12),const Text('Best action: re-check moisture before running the pump and use local field conditions as the final signal.',style:TextStyle(fontSize:12))]))]);}}
class FertilizerView extends StatelessWidget{const FertilizerView({super.key});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('NPK-aware fertilizer plan',style:TextStyle(fontSize:20,fontWeight:FontWeight.w900)),const SizedBox(height:12),const Text('Current NPK index: 82 / 48 / 42'),const SizedBox(height:14),const Text('Recommendation',style:TextStyle(fontWeight:FontWeight.w900)),const SizedBox(height:5),const Text('Prioritize a balanced crop-stage fertilizer plan and avoid nitrogen-heavy application before forecast rain. Validate exact dose using a soil test and local agronomy guidance.'),const SizedBox(height:12),const Text('Timing: after rain risk passes • Method: soil-directed application • Warning: never mix products without label compatibility checks.',style:TextStyle(fontSize:12,color:Colors.black54))]))]);}}
class SchemesView extends StatelessWidget{const SchemesView({super.key});@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(18),children:['PM-KISAN','Pradhan Mantri Fasal Bima Yojana','Soil Health Card'].map((s)=>Padding(padding:const EdgeInsets.only(bottom:10),child:AgriCard(child:ListTile(contentPadding:EdgeInsets.zero,title:Text(s,style:const TextStyle(fontWeight:FontWeight.w900)),subtitle:const Text('Eligibility, benefits and application steps should be checked against the current official government source.',style:TextStyle(fontSize:12)),trailing:const Icon(Icons.open_in_new)))).toList());}}
class GenericView extends StatelessWidget{const GenericView({super.key,required this.title});final String title;@override Widget build(BuildContext c)=>ListView(padding:const EdgeInsets.all(18),children:[AgriCard(child:Column(children:[const Icon(Icons.auto_awesome,color:AppTheme.green,size:60),const SizedBox(height:10),Text(title,style:const TextStyle(fontSize:22,fontWeight:FontWeight.w900)),const SizedBox(height:8),const Text('This module is API-ready and uses safe development data. Connect your backend/ML endpoint for live results without exposing secrets in Flutter.',textAlign:TextAlign.center,style:TextStyle(color:Colors.black54))]))]);}
